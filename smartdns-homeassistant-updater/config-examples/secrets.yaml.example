# ============================================================================
# SmartDNSProxy IP Updater - Secrets Configuration
# ============================================================================
#
# This file shows how to store your SmartDNSProxy API key in Home Assistant's
# secrets.yaml file (the recommended method for storing sensitive data)
#
# LOCATION: /config/secrets.yaml
#
# SECURITY NOTES:
# - Never commit secrets.yaml to version control
# - Never share your API key publicly
# - Home Assistant encrypts backups containing secrets.yaml
# - The API key is never logged by the update script
#

# ============================================================================
# SMARTDNS API KEY (REQUIRED)
# ============================================================================
# Replace YOUR_API_KEY_HERE with your actual SmartDNSProxy API key
# You can find this in your SmartDNSProxy account settings

smartdns_api_key: "YOUR_API_KEY_HERE"

# ============================================================================
# HOW TO USE THIS FILE
# ============================================================================
#
# 1. Copy the line above to your existing /config/secrets.yaml file
#    (Don't replace the entire file if you have other secrets!)
#
# 2. Replace YOUR_API_KEY_HERE with your actual API key from SmartDNSProxy
#
# 3. In your configuration.yaml, reference it like this:
#    shell_command:
#      smartdns_update: "bash /config/scripts/smartdns_update.sh {{ smartdns_api_key }}"
#
# 4. The {{ smartdns_api_key }} template will be replaced with the actual
#    value from this secrets.yaml file when the command runs
#
# ============================================================================
# EXAMPLE SECRETS.YAML FILE
# ============================================================================
# Your complete secrets.yaml might look like this:
#
# # Home Assistant secrets
# http_password: "my_secure_password"
# api_password: "another_password"
#
# # SmartDNS
# smartdns_api_key: "abcd1234efgh5678ijkl9012mnop3456"
#
# # Other integrations
# weather_api_key: "xyz123"
#
# ============================================================================
# GETTING YOUR API KEY
# ============================================================================
#
# 1. Log in to your SmartDNSProxy account at https://www.smartdnsproxy.com/
#
# 2. Navigate to your account settings or API section
#
# 3. Look for "API Key" or "Update API Key"
#
# 4. Copy the key and paste it here, replacing YOUR_API_KEY_HERE
#
# ============================================================================
# VALIDATION
# ============================================================================
#
# After adding your API key:
#
# 1. Check configuration:
#    Developer Tools > YAML > Check Configuration
#
# 2. If valid, reload configuration or restart Home Assistant
#
# 3. Test the setup:
#    Developer Tools > Services
#    Service: shell_command.smartdns_update
#    Click "Call Service"
#
# 4. Check logs:
#    /config/logs/smartdns_updates.log
#
# ============================================================================
# TROUBLESHOOTING
# ============================================================================
#
# If you get errors about "smartdns_api_key" not found:
# - Ensure the key name matches exactly (case-sensitive)
# - Ensure there are no extra spaces
# - Ensure secrets.yaml is in /config/ directory
# - Reload configuration after changes
#
# If updates fail with "API key appears invalid":
# - Verify you copied the complete API key
# - Check for extra spaces or quotes in the key
# - Verify the key is active in your SmartDNSProxy account
#
